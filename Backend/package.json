{
  "name": "studio-backend",
  "version": "1.0.0",
  "description": "Backend API for Hotmello Studio",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "dev:reset": "powershell -NoProfile -Command \"$conns = Get-NetTCPConnection -LocalPort 5000 -State Listen -ErrorAction SilentlyContinue; if ($conns) { $conns | Select-Object -Expand OwningProcess -Unique | ForEach-Object { Stop-Process -Id $_ -Force; Write-Output ('Killed ' + $_) } } else { Write-Output 'No listener on 5000' }; Start-Process -NoNewWindow -FilePath nodemon -ArgumentList 'server.js'\"",
    "prod": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "express-rate-limit": "^8.5.2",
    "express-validator": "^7.1.0",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.4.0",
    "mongoose": "^8.24.1",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "axios": "^1.18.1",
    "nodemon": "^3.1.4"
  }
}
